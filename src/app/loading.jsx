import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <h4 className="text-5xl text-accent font-bold">Loading...</h4>
    </div>
  );
};

export default Loading;
